---
"@aliou/pi-synthetic": minor
---

Update Pi peer dependencies to 0.72.0. Migrate `reasoningEffortMap` to `thinkingLevelMap` per Pi 0.72.0 API. Replace `session_switch` event with `session_start`. Swap `@sinclair/typebox` for `typebox`. Add Kimi K2.5 model. Set `reasoning: false` for Llama 3.3 70B.
